# Files that belong in the repository but not in an installable zip.
#
# `git archive` honours export-ignore, so the release artifact is built from the
# tag itself and cannot drift from what was committed.
/.gitattributes export-ignore
/.gitignore     export-ignore
/tests          export-ignore
